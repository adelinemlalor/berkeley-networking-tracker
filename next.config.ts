import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Hide the floating dev-tools badge so captured screenshots show only the app.
  devIndicators: false,
  /* config options here */
};

export default nextConfig;
